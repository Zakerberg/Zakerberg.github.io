{
  "name": "cherry-site-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "db:migrate": "wrangler d1 migrations apply cherry-visitors --remote",
    "deploy": "npm run db:migrate && wrangler deploy",
    "db:init": "wrangler d1 execute cherry-visitors --remote --file=./schema.sql",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.137.0"
  }
}
